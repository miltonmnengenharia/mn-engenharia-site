document.addEventListener('DOMContentLoaded',()=>{const menu=document.querySelector('[data-menu-button]');const links=document.querySelector('[data-nav-links]');if(menu&&links){menu.addEventListener('click',()=>{const open=links.classList.toggle('open');menu.setAttribute('aria-expanded',String(open));});links.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{links.classList.remove('open');menu.setAttribute('aria-expanded','false');}));document.addEventListener('click',e=>{if(!links.contains(e.target)&&!menu.contains(e.target)){links.classList.remove('open');menu.setAttribute('aria-expanded','false');}});}const form=document.querySelector('[data-whatsapp-form]');if(form){form.addEventListener('submit',e=>{e.preventDefault();const get=name=>{const el=form.elements.namedItem(name);return el?String(el.value||'').trim():''};const lines=['Olá, vim pelo site da MN Engenharia e gostaria de solicitar um orçamento.','',get('nome')?`Nome: ${get('nome')}`:'',get('telefone')?`Telefone: ${get('telefone')}`:'',get('cidade')?`Cidade: ${get('cidade')}`:'',get('servico')?`Serviço: ${get('servico')}`:'',get('tipo')?`Tipo do imóvel/obra: ${get('tipo')}`:'',get('fase')?`Fase atual: ${get('fase')}`:'',get('metragem')?`Metragem aproximada: ${get('metragem')}`:'',get('mensagem')?`Principal necessidade: ${get('mensagem')}`:'',get('detalhes')?`Detalhes: ${get('detalhes')}`:''].filter(Boolean);window.open('https://wa.me/5564993191135?text='+encodeURIComponent(lines.join('\n')),'_blank','noopener');});}});